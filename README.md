# Spell Shift

Small Three.js prototype space for validating the modular hero character asset pipeline.

## Character Test Lab

```bash
npm install
npm run dev
```

Open the Vite URL and use the on-screen controls to preview hero animations. Move with WASD or arrow keys, and use number keys to trigger animation clips.
