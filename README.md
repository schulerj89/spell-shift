# Spell Shift

Sprite-based playground for the Spell Shift character prototype.

```bash
npm install
npm run dev
```

Open the Vite URL and use the on-screen HUD plus WASD/arrow keys to move in the 2D playground.
